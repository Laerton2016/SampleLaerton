@model IEnumerable<WebAspNet_Core.Models.TokenServiceProviderConfig>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParametersJson)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParametersJson)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TokenServiceProviderCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TokenServiceProviderCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TokenServiceProviderCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
